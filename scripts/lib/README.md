# Script Library

This directory contains shared shell helpers for the repository's executable
scripts.

Current file:

- `common.sh`: argument validation, env loading, `passwordeval` assembly, and
  template rendering helpers
