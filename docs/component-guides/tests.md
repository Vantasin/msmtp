# Tests Directory Guide

[`tests/`](../../tests/) contains repository verification assets.

Current file:

- [`test.sh`](../../tests/test.sh): shell smoke tests for quickstart,
  interactive setup, multi-account render, copy install, and symlink install
  behavior

Run them with:

```bash
make check
```
