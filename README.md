# msmtp

Cross-platform msmtp configuration repo for Linux and macOS, using .env.example templates, a quick-start curl setup script, Makefile-driven install/update/test commands, and flexible passwordeval support for macOS Keychain, Linux GPG, or secure root-owned password files.